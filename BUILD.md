# Build

A [Rust toolchain](https://rustup.rs/) is required to compile from source.

```sh
cargo build --release
```

The binary is `target/release/bdmount`.

```sh
scripts/install.sh      # build a release binary and copy it to ~/.local/bin/bdmount
```
